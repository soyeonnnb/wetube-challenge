extends ../base

block content 
    h3 Hello my Channel
    div 
        if channel.avatarUrl 
            span #{channel.avatarUrl}
        span #{channel.name}
        if loggedIn && loggedInChannel._id === channel.id
            a(href="/channel/edit") 채널 수정
    ul 
        li 
            a(href="featured") 홈
        li 
            a(href="community") 커뮤니티
        li 
            a(href="about") 정보
        li 
            input(type="text" name="channel-search")
    div 
        block channelMain